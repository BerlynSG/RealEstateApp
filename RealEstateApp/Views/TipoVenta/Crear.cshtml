@using RealEstateApp.Core.Application.ViewModels.TipoVenta
@model SaveTipoVentaViewModel
@{
    bool editMode = Model.Id > 0;
    ViewData["Title"] = editMode ? "Editar Tipo de Venta" : "Crear Tipo de Venta";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-6 offset-3">
            <form method="post" enctype="multipart/form-data" asp-controller="TipoVenta"
                  asp-action="@(editMode ? "Editar" : "Crear")">
                <div class="card">
                    <div class="card-header bg-dark text-light">
                        <h4>@ViewData["Title"]</h4>
                    </div>
                    <div class="card-body">
                        @Html.ValidationSummary(false, "", new { @class = "text-danger" })
                        <div class="mb-3">
                            <label asp-for="Nombre" class="form-label">Precio:</label>
                            <input asp-for="Nombre" class="form-control" />
                            <span asp-validation-for="Nombre" class="text-danger"></span>
                        </div>
                        <div class="mb-3">
                            <label asp-for="Descripcion" class="form-label">Descripción:</label>
                            <textarea asp-for="Descripcion" class="form-control"></textarea>
                            <span asp-validation-for="Descripcion" class="text-danger"></span>
                        </div>
                        <input type="hidden" asp-for="Id" />
                    </div>
                    <div class="card-footer">
                        <button type="submit" class="btn btn-primary">
                            @(editMode ? "Guardar Cambios" : "Crear Tipo de Venta")
                        </button>
                        <a asp-controller="TipoVenta" asp-action="Index" class="btn btn-warning">Volver atrás</a>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
